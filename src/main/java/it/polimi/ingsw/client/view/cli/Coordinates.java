package it.polimi.ingsw.client.view.cli;

/**
 * A simple way of storing two integers row column coordinates
 * @param row The row that wants to be stored
 * @param column The column that wants to be stored
 */
public record Coordinates(int row, int column) {
}
